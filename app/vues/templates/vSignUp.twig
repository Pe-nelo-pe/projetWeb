<section>
  <h1>{{ titre|upper }}</h1>
  <p class="messageRetourAction {{ classRetour }}">{{ messageRetourAction|raw}}&nbsp;</p>   
  
  <form method="POST" action="session?entite=user&action=a">

    <label>Prénom</label>
    <input type="text" name="user_firstName" value="{{ user.user_firstName }}">
    <span>&nbsp;{{ erreurs.user_firstName }}</span>

    <label>Nom</label>
    <input type="text" name="user_lastName" value="{{ user.user_lastName }}">
    <span>&nbsp;{{ erreurs.user_lastName }}</span>

    <label>Courriel</label>
    <input type="email" name="user_email" value="{{ user.user_email }}">
    <span>&nbsp;{{ erreurs.user_email }}</span>

    <label>Mot de passe</label>
    <input type="password" name="user_password" value="{{ user.user_password }}">
    <span>&nbsp;{{ erreurs.user_password }}</span>

    <label>Adresse</label>
    <input type="text" name="user_address" value="{{ user.user_address }}">
    <span>&nbsp;{{ erreurs.user_address}}</span>

    <label>Ville</label>
    <input type="text" name="user_city" value="{{ user.user_city }}">
    <span>&nbsp;{{ erreurs.user_city }}</span>

    <label>Code postal</label>
    <input type="text" name="user_zipCode" value="{{ user.user_zipCode }}">
    <span>&nbsp;{{ erreurs.user_zipCode }}</span>



    <input type="submit" value="Envoyer">

  </form>
</section>
